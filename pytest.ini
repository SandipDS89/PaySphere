[pytest]
pythonpath = .
markers =
    unit: fast tests with no database or browser
    integration: tests using the Flask test client and test database
    selenium: browser-based end-to-end tests, requires a running Flask server